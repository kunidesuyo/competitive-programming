#include <bits/stdc++.h>
#include <cmath>

using ll = long long;
const ll INF = __LONG_LONG_MAX__;

using namespace std;

int main() {
    int x, a;
    cin >> x >> a;

    if(x < a) {
        cout << 0 << endl;
    } else {
        cout << 10 << endl;
    }

    return 0;
}