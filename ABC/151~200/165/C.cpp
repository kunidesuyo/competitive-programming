#include <bits/stdc++.h>
#include <cmath>

using ll = long long;
const ll INF = __LONG_LONG_MAX__;

using namespace std;

int main() {
    int n, m, q;
    cin >> n >> m >> q;
    int x[q][4];
    for(int i = 0; i < q; i++) {
        for(int j = 0; j < 4; j++) {
            cin >> x[i][j];
        }
    }

    int ans = 0;

    for(int i = 0; i < )
    

    return 0;
}