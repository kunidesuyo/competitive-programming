#include <bits/stdc++.h>
#include <cmath>

#define all(x) (x).begin(),(x).end()


const int inf = INT_MAX / 2;

using namespace std;

int main() {
    int dist[10];
    cout << inf << endl;
    memset(dist, inf, sizeof(dist));

    for(int i = 0 ; i < 10; i++) {
        cout << dist[i] << " ";
    }
    cout << "\n";  

    return 0;
}